services:
  bluetooth_audio:
    image: bluetooth-audio-test-arm64
    platform: linux
    network_mode: "host"
    volumes:
      - /data:/data
    # Temporary fix, will need to turn off privledges and track specific device paths instead
    privileged: true
    #devices:
    #  - /dev/bus:/dev/bus
    restart: unless-stopped
